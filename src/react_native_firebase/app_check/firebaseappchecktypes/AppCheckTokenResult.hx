package react_native_firebase.app_check.firebaseappchecktypes;

/**
	Result returned by `getToken()`.
**/
typedef AppCheckTokenResult = {
	/**
		The token string in JWT format.
	**/
	final token : String;
};